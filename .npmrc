# Force installation of platform-specific optional dependencies
# This is critical for Heroku deployments where native modules are required
ignore-platform=false

# Ensure we use exact versions from lockfile
save-exact=true

# Use the lockfile for CI builds
package-lock=true 