# Prevent optional dependencies from causing issues
optional=false
omit=optional

# Use legacy peer deps for compatibility
legacy-peer-deps=true

# Ensure consistent install strategy
install-strategy=nested 