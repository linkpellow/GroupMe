# Example environment configuration for Crokodial Server
# Copy this file to .env or .env.local and fill in real values for production or development

# JWT secret (must be at least 32 characters)
JWT_SECRET=your_super_secret_random_string_of_at_least_32_characters

# MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@host/dbname?retryWrites=true&w=majority

# Node environment (development or production)›
NODE_ENV=development

# Port for the server to listen on
PORT=3005

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Textdrip API token (if using Textdrip integration)
TEXTDRIP_API_TOKEN=dummy
TEXTDRIP_PHONE=dummy

# GroupMe OAuth credentials (if using GroupMe integration)
GROUPME_CLIENT_ID=your_groupme_client_id
GROUPME_REDIRECT_URI=http://localhost:5173/groupme/callback 