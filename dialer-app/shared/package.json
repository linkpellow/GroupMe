{
  "name": "crokodial-shared",
  "version": "1.0.0",
  "description": "Shared utilities and configurations for Crokodial CRM",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.3.2"
  }
} 